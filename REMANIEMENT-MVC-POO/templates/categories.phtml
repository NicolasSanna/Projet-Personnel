<h3> Catégories </h3>

<?php if (!empty($categories)):?>

    <?php foreach ($categories as $categorie):?>

        <a class="" href="<?=buildUrl('category', ['id' => intval($categorie['id'])]);?>"><?=htmlspecialchars($categorie['category']);?></a>

    <?php endforeach;?>

<?php endif;?>