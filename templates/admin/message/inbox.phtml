<h3>Messagerie</h3>

<a id="inboxAjax" class="Link" href="<?=buildUrl('inboxAjax');?>">Rafraichir</a>

<div class="MyMessages">

    <?php include TEMPLATE_DIR . '/admin/message/adminmessages.phtml';?>

    <?php include TEMPLATE_DIR . '/admin/message/table.phtml';?>

        <tbody id="tbody">

            <?php if (!empty($messages)):?>

                <?php foreach ($messages as $message):?>

                    <tr>
                        <td><?=htmlspecialchars($message['pseudoFrom']);?></td>
                        <td><?=htmlspecialchars($message['pseudoTo']);?></td>
                        <td><?=htmlspecialchars($message['subject']);?></td>
                        <td><?=$message['publication_date'];?></td>
                        <td><a href="<?=buildUrl('message', ['id' => $message['id_message']]);?>">Lire</a></td>
                        <td><a href="<?=buildUrl('messageTrashToUser', ['id' => $message['id_message']]);?>">Supprimer</a></td>
                    </tr>

                <?php endforeach;?>

            <?php endif;?>
            
        </tbody>
    </table>
</div>

<script src="<?=asset('js/inbox.js');?>"></script>